# 🚀 ШВИДКИЙ СТАРТ - iOS Build для TestFlight

## ✅ ВСЕ ГОТОВО! Проект налаштовано правильно

### Що вже зроблено:
- ✅ `eas.json` - конфігурація build профілів
- ✅ `app.json` - iOS налаштування з permissions
- ✅ `expo-dev-client` - встановлено
- ✅ `expo-build-properties` - встановлено
- ✅ Bundle ID: `com.amunx.app`
- ✅ Немає помилок у package.json

---

## 📋 Що треба зробити (5 хвилин):

### 1️⃣ Встанови EAS CLI (один раз)
```bash
npm install -g eas-cli
```

### 2️⃣ Залогінься в Expo
```bash
cd C:\Main\AmunX\mobile
eas login
```
Введи свій Expo email і пароль (або створи аккаунт на expo.dev)

### 3️⃣ Ініціалізуй project
```bash
eas init
```
Це створить Expo project і додасть project ID в app.json

### 4️⃣ Створи iOS build
```bash
eas build --profile development --platform ios
```

**Відповідай на питання:**
- Generate new credentials? → **Yes** (якщо перший раз)
- Bundle identifier: → `com.amunx.app` ✅ (вже налаштовано)

⏱️ **Час очікування:** 15-20 хвилин

---

## 📱 Після збірки:

### Варіант 1: Через TestFlight (найпростіше)
```bash
eas submit -p ios --latest
```
Потім:
1. Йди на https://appstoreconnect.apple.com
2. Знайди AmunX → TestFlight
3. Додай себе як тестера
4. Встанови TestFlight на iPhone
5. Встанови AmunX

### Варіант 2: Скачай .ipa напряму
1. Після build отримаєш посилання
2. Скачай `.ipa` файл
3. Встанови через Xcode або Apple Configurator

---

## 🎯 Використання:

1. Відкрий **AmunX** на iPhone (не Expo Go!)
2. У CMD на комп'ютері:
   ```bash
   cd C:\Main\AmunX\mobile
   npx expo start --dev-client
   ```
3. Відскануй QR код **в додатку AmunX**
4. Готово! Тестуй додаток 🎉

---

## ⚠️ Потрібно мати:

- ✅ Expo аккаунт (безкоштовно на expo.dev)
- ✅ Apple Developer аккаунт ($99/рік) - для TestFlight
- ✅ Інтернет (для EAS build на серверах Expo)

---

## 🆘 Якщо щось не працює:

**Помилка credentials:**
```bash
eas credentials
# Вибери iOS → Development → Manage credentials
```

**Помилка build:**
- Перевір логи на https://expo.dev
- Подивись на помилку і напиши мені

**Не встановлюється на iPhone:**
- Перевір що iPhone додано в Apple Developer Portal
- Перевір що правильний bundle ID

---

## 🎉 ГОТОВИЙ ПОЧИНАТИ?

```bash
cd C:\Main\AmunX\mobile
eas login
eas build --profile development --platform ios
```

**Успіхів! 🚀**

---

📖 Детальна інструкція: `BUILD_IOS_GUIDE.md`

