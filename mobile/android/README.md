# Android project stub

Run `npx react-native init AmunXMobile --version 0.74.3` and replace the generated `android` folder with this placeholder to obtain a full buildable Android project. Native modules (headset detector, audio processing) will live under `android/app/src/main/java/...`.

