# iOS project stub

Run `npx react-native init AmunXMobile --version 0.74.3` and replace the generated `ios` folder with this placeholder to obtain a full Xcode workspace. Native audio + LiveKit integrations will live under `ios/AmunX`.

