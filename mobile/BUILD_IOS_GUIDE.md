# 📱 Створення iOS Development Build для TestFlight

## ✅ Що вже готово:

- ✅ `expo-dev-client` встановлено
- ✅ `eas.json` створено з профілями build
- ✅ `app.json` налаштований для iOS
- ✅ Bundle ID: `com.amunx.app`
- ✅ Всі необхідні плагіни додано

---

## 🚀 Покрокова інструкція:

### **Крок 1: Встанови EAS CLI (якщо ще немає)**

```bash
npm install -g eas-cli
```

### **Крок 2: Залогінься в Expo**

```bash
cd C:\Main\AmunX\mobile
eas login
```

Введи свій Expo аккаунт (email/username і пароль)

---

### **Крок 3: Створи Expo Project (якщо потрібно)**

```bash
eas init
```

Це створить project ID який автоматично додасться в `app.json`

---

### **Крок 4: Налаштуй iOS credentials**

```bash
eas credentials
```

Вибери:
- iOS
- Development Build
- Дозволь EAS створити нові credentials або використай існуючі

---

### **Крок 5: Створи Development Build**

```bash
eas build --profile development --platform ios
```

**Що станеться:**
1. EAS завантажить код на сервер
2. Збудує iOS app з expo-dev-client
3. Створить `.ipa` файл
4. Дасть тобі посилання для скачування

**Час збірки:** ~15-20 хвилин ⏱️

---

### **Крок 6: Встанови на iPhone**

**Варіант A: Через TestFlight (рекомендовано)**

1. Після збірки натисни `eas submit -p ios --latest`
2. Введи Apple ID credentials
3. Build автоматично завантажиться в App Store Connect
4. Додай тестерів в TestFlight
5. Встанови через TestFlight на iPhone

**Варіант B: Напряму через розробницький профіль**

1. Скачай `.ipa` файл з EAS dashboard
2. Відкрий у Xcode або Apple Configurator
3. Встанови на пристрій

---

## 📱 Використання після встановлення:

1. **Відкрий AmunX app** на iPhone (не Expo Go!)
2. **У CMD вікні запусти:**
   ```bash
   npx expo start --dev-client
   ```
3. **Відскануй QR код** у додатку AmunX (не Expo Go!)
4. **Додаток завантажиться** з Metro bundler

---

## 🔧 Альтернатива: Локальний build (якщо є Mac)

Якщо маєш Mac з Xcode:

```bash
npx expo run:ios
```

Це збудує локально і запустить на симулятор/підключений iPhone

---

## ⚡ Швидкий старт (все в одній команді):

```bash
cd C:\Main\AmunX\mobile
eas login
eas build --profile development --platform ios --auto-submit
```

Це:
- Залогінить
- Збудує
- Автоматично завантажить в TestFlight

---

## 🐛 Що робити якщо помилки:

### **"No bundle identifier"**
```bash
# Оновіть app.json - вже зроблено ✅
```

### **"Missing credentials"**
```bash
eas credentials
# Створи нові або використай існуючі
```

### **"Build failed"**
```bash
# Перевір логи на https://expo.dev/accounts/[твій-username]/projects/amunx/builds
```

---

## 📊 Очікувані результати:

### **Development Build міститиме:**
- ✅ React Native 0.74.5
- ✅ Expo SDK 54
- ✅ Всі нативні модулі (LiveKit, expo-av, notifications, тощо)
- ✅ Hot reload через Metro bundler
- ✅ Debug меню (shake device)

### **Після встановлення зможеш:**
- ✅ Запускати на реальному iPhone
- ✅ Тестувати мікрофон, камеру, push сповіщення
- ✅ Live reload при змінах коду
- ✅ Дебагити через React DevTools

---

## 💰 Вартість:

- **EAS Build:** Free tier (30 builds/місяць для iOS)
- **TestFlight:** Безкоштовно
- **Apple Developer Account:** $99/рік (потрібен для TestFlight)

---

## 🎯 Висновок:

Після успішного build та встановлення через TestFlight, ти зможеш:

1. ✅ Відкривати додаток на iPhone (нативний, не Expo Go)
2. ✅ Підключатися до Metro bundler через QR код
3. ✅ Тестувати всі нативні фічі без обмежень Expo Go
4. ✅ Отримувати оновлення коду через hot reload

---

**Готовий почати? Запусти:**

```bash
cd C:\Main\AmunX\mobile
eas login
eas build --profile development --platform ios
```

🚀 **Успіхів з build!**

