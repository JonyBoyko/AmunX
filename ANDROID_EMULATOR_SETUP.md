# 🤖 Як запустити AmunX на Android емуляторі

## 📋 ШВИДКА ІНСТРУКЦІЯ

### Крок 1: Перевір чи є Android Studio

Відкрий PowerShell і запусти:
```powershell
Get-Command adb
```

Якщо **знайдено** - Android Studio встановлений! ✅  
Якщо **помилка** - треба встановити ⬇️

---

## 🔧 ВСТАНОВЛЕННЯ (якщо немає Android Studio)

### 1. Завантаж Android Studio
https://developer.android.com/studio

**Розмір:** ~1GB  
**Час установки:** ~15-20 хвилин

### 2. Встанови Android Studio
- Запусти інсталятор
- ✅ Вибери "Standard" installation
- ✅ Встанови Android SDK
- ✅ Встанови Android Virtual Device (AVD)

### 3. Налаштуй змінні оточення

Додай в `PATH`:
```
C:\Users\[ТвійUser]\AppData\Local\Android\Sdk\platform-tools
C:\Users\[ТвійUser]\AppData\Local\Android\Sdk\emulator
```

**Як додати:**
1. Натисни `Win + Pause/Break`
2. "Advanced system settings"
3. "Environment Variables"
4. В `Path` додай ці шляхи

### 4. Створи віртуальний пристрій (AVD)

Відкрий Android Studio:
1. "More Actions" → "Virtual Device Manager"
2. "Create Device"
3. Вибери: **Pixel 6 Pro** (рекомендую)
4. System Image: **Android 13 (API 33)** або вище
5. Finish!

---

## 🚀 ЗАПУСК AmunX НА ЕМУЛЯТОРІ

### Варіант A: Через Expo (найпростіше)

```bash
cd C:\Main\AmunX\mobile
npx expo run:android
```

**Що станеться:**
1. Expo знайде емулятор
2. Запустить його автоматично
3. Зібудує development build
4. Встановить на емулятор
5. Запустить додаток з hot reload!

⏱️ **Час:** 5-10 хвилин перший раз, потім 1-2 хвилини

---

### Варіант B: Ручний запуск

#### 1. Запусти емулятор вручну
```bash
emulator -avd [Назва_Твого_AVD] -netdelay none -netspeed full
```

Або через Android Studio:
- Tools → Device Manager → ▶️ Play

#### 2. Перевір що емулятор запущений
```bash
adb devices
```

Має показати:
```
List of devices attached
emulator-5554   device
```

#### 3. Запусти Expo
```bash
cd C:\Main\AmunX\mobile
npx expo start
```

#### 4. Натисни `a` в консолі
Або відскануй QR код в Expo Go (але не спрацює через RN 0.74.5)

---

## 🎯 РЕКОМЕНДОВАНИЙ СПОСІБ

### Якщо Android Studio ВПЕРШЕ:
```bash
# 1. Встанови Android Studio (15-20 хв)
# 2. Створи AVD через GUI (5 хв)
# 3. Запусти:
cd C:\Main\AmunX\mobile
npx expo run:android
```

### Якщо Android Studio вже є:
```bash
cd C:\Main\AmunX\mobile
npx expo run:android
```

**І ВСЕ!** 🎉

---

## ✅ ПЕРЕВАГИ ЕМУЛЯТОРА

✅ **Безкоштовно** - не потрібен платний аккаунт  
✅ **Швидко** - 5-10 хвилин setup  
✅ **Hot reload** - змінюй код і одразу бачиш результат  
✅ **Debugging** - Chrome DevTools працюють  
✅ **Всі фічі** - мікрофон, камера, геолокація (симульовані)  

---

## ⚠️ АЛЬТЕРНАТИВА БЕЗ ANDROID STUDIO

### Windows Subsystem for Android (WSA)
Якщо маєш **Windows 11**:

1. Відкрий Microsoft Store
2. Встанови "Windows Subsystem for Android"
3. Запусти будь-який Android додаток
4. Встанови `.apk` через `adb install`

**НО:** Складніше налаштувати для розробки.

---

## 🆚 ПОРІВНЯННЯ ВАРІАНТІВ

| Метод | Час | Складність | Hot Reload | Вартість |
|-------|-----|-----------|-----------|----------|
| **Android емулятор** | 5-10 хв | Легко | ✅ Так | ✅ Безкоштовно |
| EAS Android build | 15-20 хв | Дуже легко | ❌ Ні | ✅ Безкоштовно |
| EAS iOS build | 15-20 хв | Легко | ❌ Ні | ❌ $99/рік |
| Expo Go | 1 хв | Дуже легко | ✅ Так | ❌ Не працює (RN 0.74.5) |
| Web версія | 1 хв | Дуже легко | ✅ Так | ❌ Не працює (@expo/vector-icons) |

---

## 🎬 ПОКРОКОВА ПОСЛІДОВНІСТЬ ДІЙ

### Якщо Android Studio немає:

```bash
# 1. Завантаж і встанови Android Studio (20 хв)
# Іди на: https://developer.android.com/studio

# 2. Запусти Android Studio, створи AVD (5 хв)
# Tools → Device Manager → Create Device → Pixel 6 Pro → Android 13

# 3. Закрий Android Studio

# 4. Запусти в PowerShell:
cd C:\Main\AmunX\mobile
npx expo run:android
```

### Якщо Android Studio вже є:

```bash
# Просто запусти:
cd C:\Main\AmunX\mobile
npx expo run:android
```

---

## 🔍 ПЕРЕВІРКА ЧИ ВСЕ ВСТАНОВЛЕНО

```bash
# Перевір Java
java -version

# Перевір Android SDK
adb version

# Перевір емулятори
emulator -list-avds
```

Якщо всі команди працюють - **готовий до запуску!** ✅

---

## 💡 ПІДКАЗКИ

### Якщо емулятор не запускається:
```bash
# Перезапусти ADB
adb kill-server
adb start-server
```

### Якщо Metro bundler помилка:
```bash
# Очисти кеш
npx expo start --clear
```

### Якщо build помилка:
```bash
# Видали build папки
cd C:\Main\AmunX\mobile
Remove-Item -Recurse -Force android\app\build, android\.gradle
npx expo run:android
```

---

## 🎯 ФІНАЛЬНА КОМАНДА

```bash
cd C:\Main\AmunX\mobile
npx expo run:android
```

**Цього достатньо якщо Android Studio встановлений!** 🚀

---

**Удачі! Через 10 хвилин побачиш свій додаток на емуляторі!** 🎉

